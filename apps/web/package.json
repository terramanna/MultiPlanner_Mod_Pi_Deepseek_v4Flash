{
  "name": "multiplanner-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "node tests/bootstrap-retry.check.js && node tests/provider-coverage-cache.check.js && node tests/provider-selection.check.js && node tests/leaflet-selection.check.js",
    "dev": "vite --host 127.0.0.1 --port 5173 --configLoader native",
    "build": "vite build --configLoader native",
    "preview": "vite preview --host 127.0.0.1 --port 4173"
  },
  "dependencies": {
    "@geoman-io/leaflet-geoman-free": "^2.20.0",
    "cesium": "^1.127.0",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
