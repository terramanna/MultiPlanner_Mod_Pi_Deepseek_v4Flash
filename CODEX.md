# Codex project instructions

Read `AGENTS.md` and `CONTEXT.md` before changing this repository.

Commit using the Git identity configured for your Codex entry in
`config/commit-actors.json` and include the matching trailer:

```text
Actor: <configured Codex actor>
```

See `docs/COMMIT_AUDIT.md` for the complete policy.
