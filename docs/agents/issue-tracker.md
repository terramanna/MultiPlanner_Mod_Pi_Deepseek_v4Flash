# Issue tracker

GitHub Issues are the default work tracker.

An implementation-ready issue states the outcome, boundaries, non-goals, verifiable acceptance criteria, relevant domain terms, dependencies, and whether it is ready for an agent or needs human input.
